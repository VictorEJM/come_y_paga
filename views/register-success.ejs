<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Has sido registrado</title>
    <style>
      body {
        background-color: #f2f2f2;
        font-family: Arial, sans-serif;
      }
      .container {
        margin: 100px auto;
        max-width: 600px;
        padding: 50px;
        background-color: #fff;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        text-align: center;
      }
      h1 {
        color: #0066cc;
        font-size: 36px;
        margin-bottom: 30px;
      }
      p {
        font-size: 24px;
        margin-bottom: 30px;
      }
      .redirect {
        font-size: 18px;
        color: #666;
      }
      .redirect span {
        color: #0066cc;
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>¡Has sido registrado!</h1>
      <p>Se ha creado tu cuenta.</p>
      <p class="redirect">Serás redirigido a la página de inicio de sesión en<span id="countdown">5</span> segundos.</p>
    </div>
    <script>
      // redirect to the home page after 5 seconds
      let countdown = 5;
      setInterval(() => {
        countdown--;
        if (countdown <= 0) {
          window.location.href = '/';
        } else {
          document.getElementById('countdown').innerText = countdown;
        }
      }, 1000);
    </script>
  </body>
</html>
